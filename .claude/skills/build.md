# Build Project

Compile TypeScript and prepare for running.

## Instructions

1. Run TypeScript compiler:
   ```bash
   cd ~/Documents/sources/music-analyzer && npm run build
   ```

2. If build fails, show the errors and help fix them

3. After successful build, confirm:
   ```bash
   node dist/index.js --version
   ```
